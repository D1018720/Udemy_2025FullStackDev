<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>更新學生資料</title>
</head>
<body>
    <!-- 瀏覽器不能寄出PUT/PATCH/DELETE請求，只能寄出GET/POST -->
    <!-- 所以要用method-override -->
    <form action="/students/<%= foundStudent._id %>?_method=PUT" method="POST">
        <label for="name">名稱:</label>
        <input type="text" name="name" value="<%= foundStudent.name %>"><br>

        <label for="age">年齡:</label>
        <input type="number" name="age" value="<%= foundStudent.age %>"><br>

        <label for="major">主修:</label>
        <input type="text" name="major" value="<%= foundStudent.major %>"><br>

        <label for="merit">merit獎學金:</label>
        <input type="number" name="merit" value="<%= foundStudent.scholarship.merit %>"><br>

        <label for="other">other獎學金:</label>
        <input type="number" name="other" value="<%= foundStudent.scholarship.other %>"><br>

        <button>更新學生資料</button>
    </form>
</body>
</html>